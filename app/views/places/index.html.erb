<%= will_paginate @places %>
<% @places.each do |place| %>
  <div class="boom-box col-xs-10 col-xs-offset-1">
    <h1><%= place.name %></h1>
    <i><%= place.address %></i><br /><br />
    <p><%= place.description %><br /></p>
  </div>
<% end %>
<%= will_paginate @places %>
